server:
  port: 8081
  servlet:
    context-path: /user

spring:
  application:
    name: microuser
  cloud:
    config:
      discovery:
        enabled: true
        service-id: microconfig

management:
  endpoints:
    web:
      exposure:
        include: *